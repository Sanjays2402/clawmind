# 2026-06-21 00:40 PDT tick — Cake (cron)

Branch: feature/autoship
Pushed to: origin/feature/autoship

## Features shipped (5)

1. **`feat(related): add -t/--threshold to drop neighbours below a relevance score`** — `51dd7b8`
   - Mirrors `search --threshold` byte-for-byte (inclusive lower bound, non-numeric silently ignored, applied before paths-only / json / text). Client-side filter because /v1/related doesn't accept minScore today.
   - --json `count` recomputed; `sourceChunkCount` preserved verbatim.
   - Tests: related.test.ts 8 → 13 (+5).

2. **`feat(digest): add show --last <n> to cap history rows newest-first`** — `53e2550`
   - Sliding-window sibling of `--since`. Composes as "the newest N rows that pass -q / --since".
   - Non-positive / NaN rejected because `--last 0` would silently look like an empty history and break the unified empty-state hint.

3. **`feat(stats): add --paths to emit the per-namespace extension flat list`** — `fc363f0`
   - Pipeline-friendly flat-extension stream. No implicit dedupe so `grep -c md` counts namespaces.
   - Short-circuits --json / --tsv when set (same precedent as `search --paths-only`).

4. **`feat(watch): add --debounce <ms> to coalesce rapid file events`** — `814cbe5`
   - Forwards to the watcher's existing `debounceMs` (default 800). Zero/negative/NaN rejected because `--debounce 0` melts CPU during `git checkout` bursts.
   - Missing flag → undefined so the watcher's `?? 800` fallback keeps working (don't mask future default changes).

5. **`feat(watch): emit an NDJSON startup banner to stderr on each run`** — `76a5c9e`
   - `{"kind":"banner","root":"...","ts":"..."}` on stderr in both text + --json modes so a stderr-tailing scraper detects restarts.
   - Does NOT fire on the --debounce validation error path (no half-started process to mark).

## Tests

- `@clawmind/cli`: **216/216** vitest pass (up from 186, +30 net new).
  - related.test.ts: 8 → 13 (+5)
  - feedback-digest.test.ts: 34 → 41 (+7)
  - stats.test.ts: 32 → 38 (+6)
  - watch.test.ts: 0 → 12 (NEW)
- Typecheck: `@clawmind/cli` clean.
- Two pre-existing reds OUTSIDE cli (telemetry OpenTelemetry 1.x/2.x peer mismatch + rag/hybrid alpha-blend drift); both verified pre-existing, neither introduced this tick.

## Notes for next tick

- The watch tests pioneered a "sentinel-throwing mock" pattern for `startWatcher` to avoid the production `await new Promise(() => undefined)` deadlock during test execution. Reusable for any future cli command that ends in a deliberate hang.
- Cleaned up stray .js / .d.ts / .js.map / .d.ts.map sidecars under `apps/cli/src/commands/` at tick start (left over from a previous `tsc` invocation). Queued an idea: add `apps/cli/src/**/*.js` to .gitignore to make the trap impossible.
- Refilled the roadmap with 6 new items (watch --quiet, watch --once, digest --since-last, stats --json --since slim shape, related -n end-to-end test, stale --paths-only alias) so future ticks have a fresh stack.

## Verify gate output

```
@clawmind/cli@0.1.0 test /Volumes/Projects/clawmind/apps/cli
> vitest run

 Test Files  16 passed (16)
      Tests  216 passed (216)
   Duration  2.68s
```

```
git fetch -q origin && git log --oneline origin/feature/autoship | head -7
76a5c9e feat(watch): emit an NDJSON startup banner to stderr on each run
814cbe5 feat(watch): add --debounce <ms> to coalesce rapid file events
fc363f0 feat(stats): add --paths to emit the per-namespace extension flat list
53e2550 feat(digest): add show --last <n> to cap history rows newest-first
51dd7b8 feat(related): add -t/--threshold to drop neighbours below a relevance score
```

All 5 commits landed on origin/feature/autoship. Lock released.
