# clawmind autoship tick — 2026-06-21 07:05 PDT

5 features shipped directly on main. All committed and pushed.

## Features

1. **d951f11** `chore(repo)`: ignore stray .js/.d.ts sidecars next to apps/cli/src/*.ts
2. **d9f9dda** `feat(doctor)`: add --stale-after-days <n> end-to-end (API + CLI)
3. **fc6e3f2** `feat(ingest)`: add --dry-run + --paths-only rehearsal preview
4. **d589255** `feat(tags)`: add list --sort <count|tag> and --top <n>
5. **216077b** `feat(feedback)`: add prune --below <n> with --apply safety pattern

## Test gate

- `@clawmind/cli` vitest: **292/292** pass (up from 254, +38 net new tests)
  - doctor.test.ts: 15/15 (+6)
  - ingest.test.ts: 18/18 (+10)
  - tags.test.ts: 14/14 (+10)
  - feedback-digest.test.ts: 52/52 (+11) — new "feedback prune cli" describe block
- `@clawmind/cli` typecheck: **clean** (zero errors)
- `@clawmind/api` vitest: **1223/1223** pass — including all 6 doctor route tests
  with the new optional `?staleAfterDays=<n>` querystring schema.

## Pre-existing reds (queued, NOT introduced by this tick)

Both verified pre-existing in THIS tick by running the failing commands on a
clean working tree BEFORE the diff was committed — identical errors:

1. `@clawmind/telemetry` typecheck — OpenTelemetry 1.x/2.x peer mismatch.
   Same TS2741 + TS2322 in `src/tracing.ts` lines 12 + 16. The exporter and
   auto-instrumentations need version bumps in lockstep with @opentelemetry/resources
   ^2.0.0 to clear all peer warnings.
2. `packages/rag/test/hybrid.test.ts` — `expected 'a' to be 'b'` under
   alpha=0.5 blend. Either the test fixture or the hybrid merge has drifted;
   fix is either rewriting the test against current alpha-blend semantics or
   re-deriving the expected order from first principles.

## Theme

Knocked out THREE explicitly-queued items the queued list called out by name:
- `feat(doctor): --staleAfterDays <n>` (was queued)
- `feat(ingest): --paths-only --dry-run preview shortcut` (was queued)
- `chore(repo): .gitignore .js/.d.ts sidecars` (was queued, trap caught last tick)

Plus two scripting-ergonomics features that mirror contracts already in the cli:
- `tags list --sort/--top` mirrors `stats --sort` / `stats --top` byte-for-byte
- `feedback prune --below --apply` mirrors `feedback list --below` (strict <)
  + `forget --apply` (dry-run by default + rerun nudge + safety tripwire)

The cli's `--paths-only` contract now spans every list-style command:
search, forget, stale, related, pins, mutes, aliases, tags, reindex, and now
ingest all emit the same byte layout (one path per line, no ANSI, no headers,
no hints; empty stream on zero matches).

## Push

`504d92a..216077b main -> main` — all five commits authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`. No PRs.

## Notes for the next tick

The `--paths-only` rollout is now complete across list-style commands. The next
likely scripting-ergonomics gap is the queued list's `feat(stats): --json --slim
--tsv` shortcut and `feat(reindex): --since <iso-date>` (the dry-run preview now
exists for ingest --since but the live reindex still always wipes and re-walks).

A real cron freshness SLO can now be wired:
```
clawmind doctor --severity error --stale-after-days 1
```
plus a Sunday-morning bulk-clear:
```
clawmind feedback prune --below 0.7 --apply
```
both with full --json output for cron log aggregation.
