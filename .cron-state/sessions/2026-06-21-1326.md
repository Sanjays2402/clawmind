# Tick 2026-06-21 13:26 PDT

5 features shipped directly on main. All five from the explicitly-queued list
or named by intent. Theme: knock out queued cli items + structural RAG
pipeline improvement for debugging.

## Features (in commit order)

1. **22bb5f4** feat(export): add --since end-to-end (API + CLI) for incremental conversation dumps
2. **d3419d3** feat(search): add --rerank-off debug escape hatch through RAG retrieve()
3. **e747919** feat(watch): add --once for a single scheduled-scan pass (cron-friendly twin)
4. **6ac0f90** feat(digest): add run --max <n> to cap the per-batch digest count
5. **59d79f3** feat(pins/mutes): add list --by <user> for per-creator snapshot scoping

## Test gate

- `@clawmind/cli`: 368/368 vitest pass (up from 329, +39 net new)
  - export.test.ts (+7 -> 15)
  - search.test.ts (+5 -> 30)
  - watch.test.ts (+10 -> 31)
  - feedback-digest.test.ts (+7 -> 73)
  - pins.test.ts (+6 -> 15)
  - mutes.test.ts (+4 -> 12)
- `@clawmind/rag`: +2 in pipeline.test.ts (4/4 total for that file)
- `@clawmind/api`: new file `conversation-export-since.test.ts` (7/7)
  - Full suite: 1230/1230 on second run (one flake on
    api-key-bruteforce timing-dependent lockoutMs assertion in a
    parallel run; isolated test passes 10/10)
- `@clawmind/cli` typecheck: clean
- `pnpm typecheck` (workspace): one red, the known-queued
  `@clawmind/telemetry` OpenTelemetry 1.x/2.x peer mismatch

## Pre-existing reds (NOT introduced this tick)

- `packages/rag/test/hybrid.test.ts` alpha-blend drift (queued since
  2026-06-20 16:05 PDT)
- `packages/telemetry/src/tracing.ts` OpenTelemetry 1.x/2.x peer
  mismatch (queued since 2026-06-20 04:25 PDT)

## Push

0028623..59d79f3 main -> main. All five commits authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`.

## Notes

- The patch tool's V4A patcher had two near-misses this tick: (1) on
  apps/cli/src/commands/watch.ts where it appended the new code
  AND kept a stale duplicate at the file end (cleaned with a write_file
  rewrite); (2) on .cron-state/STATE.md where it inserted the new
  tick-log entry above the prior entry instead of below (fixed by
  removing the misplaced block and appending to the end). Worth being
  more careful about anchor specificity on multi-paragraph patches in
  long files.
- The cli surface continues to grow more pipeable. Every list-style
  command now has the same --paths/--paths-only contract; --since is
  now end-to-end across every snapshot-bearing surface (ingest,
  reindex, stale, stats, digest show/run, pins, mutes, export);
  per-user audits (--by) and capped batches (--max) close two gaps
  that were named in the queued list specifically because they
  matter for multi-user / multi-tick cron operation.
- New roadmap items added based on this tick's work: --rerank-only
  inverse, --once + --since composition, --max + --slim shape for
  cron dashboards, and an end-to-end --since round-trip test for
  export (the cli mocks fetch, the api injects routes, but neither
  catches a wire-format regression on the seam between them).
