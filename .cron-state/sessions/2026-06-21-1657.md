# 2026-06-21 16:57 PDT tick — Cake (cron)

Five features shipped directly on `main`, all explicitly-queued items.

## Commits

- 2d9f396 feat(search): add --rerank-only debug flag (skipMmr through RAG retrieve)
- c1d622f test(related): pin -n/--namespaces end-to-end forwarding contract
- b0da13a feat(digest): add run --json --slim for 3-field cron-dashboard tally
- 08fcba0 feat(forget): make --paths-only short-circuit win over --json (cron-safe combo)
- 465b833 feat(stats): add --json --slim --tsv awk-pipeline shape (2-col namespace+files)

## Test gates

- `@clawmind/cli` 393/393 (up from 368). +25 net new tests.
- `@clawmind/rag` 47/48 (the 1 fail is the queued hybrid alpha-blend
  red; my 4 new skipMmr tests pass). pipeline.test.ts now 8 tests
  (up from 4) — the rerank-only test plus 3 supporting regressions.
- `@clawmind/api` 1230/1230 on second run (snapshots prune-arithmetic
  flake on one parallel run; isolated to 11/11; not introduced this
  tick — I did not touch apps/api).
- `@clawmind/cli` typecheck clean.
- `pnpm typecheck` red is the queued telemetry OpenTelemetry 1.x/2.x
  peer mismatch.

## Theme

Explicit-queue sweep. FIVE of five features were named in the queued
list by exact shape:
1. The --rerank-only flag inverts --rerank-off into a 3-way A/B over
   the RAG pipeline (default rerank+MMR / no-rerank / no-MMR).
2. The related namespaces regression test pins a contract that has
   been live since the command first shipped with no test coverage.
3. The digest run --slim mirrors `doctor --json --quiet` byte-for-byte
   for cron dashboard panels.
4. The forget paths-only short-circuit fixes a fragile-script edge
   case (operators who always pass --json for safety).
5. The stats --json --slim --tsv emits the awk-pipeline 2-col shape
   on the slim path.

## Roadmap state

5 items shipped off the queue. Refilled with 4 new items:
- search --rerank-only --json --no-snippet byte-shape regression
- related --above/--below mirroring `feedback list`
- stats --json --slim --since byte-layout regression
- digest run --json --slim --since triple-flag byte-layout regression

Queue currently has 14 items.

## Push

Push: 0028623..465b833 main -> main. All five commits authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`. No
PRs, no tags. Verify: git log --oneline origin/main confirms all
5 SHAs landed.
