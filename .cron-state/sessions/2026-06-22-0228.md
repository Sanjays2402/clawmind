# Tick 2026-06-22 02:28 PDT — Cake (cron)

5 features shipped directly on main. All five named in the queued list at
tick start.

## Commits

- `c9490bc` feat(status): --watch --json embeds a monotonic cycle:N per snapshot
- `c9d27f5` feat(ask): --out - treats single hyphen as the stdout sentinel
- `9748183` feat(watch): --once --preview-json wraps the preview list in a structured envelope
- `dc329f5` test(ask): pin --stream-json --no-citations --out 3-flag composition byte layout
- `fea8312` test(related): pin --below / --above + --below + --paths-only byte layout

Push: `f929901..fea8312 main -> main`. All five authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`.

## Test gate

- `@clawmind/cli` 473/473 vitest pass (up from 450; +23 net new)
  - status.test.ts: +4 → 29 (cycle:N counter, including mixed-probe-state increment guard)
  - ask.test.ts: +6 → 30 (3 stdout-sentinel + 3 stream-json/no-citations/out byte pins)
  - watch.test.ts: +9 → 56 (full --preview-json contract incl. precedence + validation)
  - related.test.ts: +4 → 28 (--below + --above-below band-filter --paths-only pins)
- `@clawmind/cli` typecheck: clean
- `@clawmind/rag` test: 47/48 (queued hybrid alpha-blend; verified pre-existing)

## Notes for next tick

- Commit hygiene incident: feature 3's first commit attempt had a typo in the
  cron email (extra `+` between `Sanjays2402` and `@`). Caught immediately by
  `git log --format='%ae' -1` post-commit, amended cleanly with
  `--reset-author` before pushing. origin/main only sees the clean SHA
  (`9748183`). Worth thinking about a pre-push verification step.
- All three pre-existing reds across the monorepo remain queued and unchanged:
  - telemetry OpenTelemetry 1.x/2.x peer mismatch (typecheck)
  - rag/hybrid alpha-blend test (47/48 every tick)
  - api-key-bruteforce timing flake (1229/1230 in parallel; 10/10 in isolation)
- Roadmap refill: 5 new queued items added covering both feature ideas
  (--check-after mid-loop reset pin, --stream-json --out append-vs-truncate
  pin, --once --preview-json jq-friendly pin, --watch --json cycle off-by-one
  pin, --out - + --threshold skip composition) and one structural pin (the
  band-filter --json byte-layout that mirrors the new --paths-only pins).

## Theme

Cross-mode primitives + cross-composition regression-guard pins. The cli's
cron surface grew three new primitives (monotonic stream ordering, stdout-
sentinel convention, dashboard-shape preview) plus two byte-layout
regression-guard pins for compositions that earlier ticks had shipped but
never anchored at the SHAPE level. Each new flag composed cleanly with 1-2
existing flags; each pure-pin commit closed a gap a prior feature ship had
left open.
