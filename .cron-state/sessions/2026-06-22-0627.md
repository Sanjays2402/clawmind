# 2026-06-22 06:27 PDT — Cake (cron) 20-min batch

## Summary

5 features shipped directly on `main`, pushed cleanly to
`origin/main` (`79d3708..e5e73db`). `@clawmind/cli` test gate
498/498 vitest pass (was 473 → +25 net new tests). Typecheck
clean on the cli package.

## Features

1. `f2bb590` feat(stale): `--tsv --header` for typed-table parsers.
   Single tab-separated schema row (`path\tageDays\tchunkCount\tsize`)
   prepended to the body when `--header` is set. Fires
   UNCONDITIONALLY (including the empty-body case so
   `pandas.read_csv` produces a valid empty table). Silent-ignore
   under --json/--paths/--paths-only/text. 5 new tests pin the
   byte-layout contract + the schema-row-is-the-contract
   invariant + the --since composition.

2. `1e51e5d` feat(feedback): `list --top <n>` ranks by
   |boost - 1.0| descending. Answers "which votes are LOUDEST
   regardless of direction" — a question --above/--below cannot
   answer in a single call. Distances snapped to 6dp to dodge
   FP error de-tying genuinely-equal entries. Original index as
   secondary tie-breaker for deterministic snapshots. 5 new
   tests pin the contract (mixed-direction loudness ordering,
   downvotes outranking upvotes at same |distance|, --above
   intersection, --top 0 clamping, text/JSON parity).

3. `b7dd8c6` feat(digest): `list --since <iso-date>` surfaces
   overdue saved searches. Strict less-than (<), never-run
   digests always included. Mirrors `digest run --since` so a
   dashboard probe + the run command consume the same cutoff.
   5 new tests pin the contract + -q composition.

4. `27c6fc2` feat(aliases): `list --since <iso-date>` completes
   the --since family. aliases was the last list-style command
   missing the flag (pins/mutes/stats/stale/digest list/digest
   show all already had it). Cutoff INCLUSIVE (>=) mirroring
   pins/mutes. 5 new tests pin the contract + --paths
   composition.

5. `e5e73db` feat(tags): `paths <tag> --paths-only` alias for
   `--paths`. Last list-style command whose pipeline-friendly
   emit was named differently from the family-wide canonical
   `--paths-only` (search/forget/related/stale all expose
   `--paths-only`). True alias — both flags emit identical
   byte streams. 5 new tests pin the alias relationship
   byte-for-byte.

## Test results

- `@clawmind/cli`: 498/498 pass on retry (+25). First run hit
  known timing flake on status `--check-after`, clean on retry.
- `@clawmind/cli` typecheck: clean.
- `@clawmind/rag`: 47/48 (pre-existing hybrid alpha-blend red,
  queued since 2026-06-20 16:05 PDT, not touched this tick).
- Telemetry typecheck red queued since 2026-06-20 04:25 PDT,
  not touched this tick.

## Push verification

```
79d3708..e5e73db  main -> main
```

All 5 commits authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`.

## Theme

Extend cron-friendly surface in ways that close real ergonomic
gaps OR complete cross-command family contracts. Three brand-new
capabilities (header-row TSV, loudness ranking, overdue probe);
two family-contract extensions (--since on aliases, --paths-only
on tags paths) that close mismatches earlier ticks left in place.
Each new flag composes cleanly with 1-2 existing flags without
requiring any other command change.
