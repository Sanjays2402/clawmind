# 2026-06-22 10:07 PDT (Cake/cron)

## Result
5 features shipped + pushed directly to main. Test gate: `@clawmind/cli` 530/530 (up from 498). Typecheck clean. Push: `ce05108..85add58 main -> main`.

## Features (5)
- **1f9946a** `feat(feedback): list --sort <boost|path|ups|downs> orders survivors before --top`
  - Explicit ordering primitive distinct from --top (which ranks by |boost-1.0|).
  - --sort downs --top 10 = "the 10 most-downvoted paths" (different question than --top 10 alone).
  - Bug caught on first test run: --top was re-sorting by distance after --sort. Fix: short-circuit --top to slice(0, N) when --sort is set.
- **c6cd0e7** `feat(digest): list --sort <lastRunTs|runs|title> orders survivors of -q/--since`
  - Canonical cron use: `digest list --since "..." --sort lastRunTs --json` for overdue audit, oldest-first.
  - Critical: lastRunTs === null sorts to TOP (mapped to -Infinity) matching the --since precedent.
- **71d2f59** `feat(aliases): list --sort <name|createdAt> orders survivors of -q/--since`
  - --sort createdAt + --since = daily snapshot of new aliases, newest-first.
- **b99dbcf** `feat(pins,mutes,aliases): list --paths-only as family-wide alias for --paths`
  - Three commands one commit (naming completion across a contract family).
  - Original --paths kept for backwards compat; both flags byte-identical via `if (opts.paths || opts.pathsOnly)`.
- **85add58** `feat(related): --sort <score|path|namespace> orders survivors of band-filter`
  - --sort namespace groups neighbours alphabetically by namespace + secondary by API order within each namespace.
  - Composes with --above/--below for "show me strong neighbours grouped by namespace".

## Theme
Eighth consecutive ship-from-patterns tick. The cli's cron surface gained ONE new ordering primitive (--sort, spanning 4 list-style commands + related's band-filter ordering) and ONE family-wide naming completion (--paths-only on pins/mutes/aliases — the last three list-style commands that lacked the canonical spelling).

The --sort design across all four commands now follows a uniform contract that any future list-style command can mirror byte-for-byte:
- Applied AFTER narrowing filters
- Secondary sort by original index for cross-snapshot determinism
- Unknown keys abort cleanly with exit 1
- Default preserves API order so `--json` diffs stay byte-stable

The composition rule "filters narrow, --sort orders, --top caps" is now consistent across every command that has all three.

## Tests
- feedback-digest.test.ts: +10 (91 -> 101) — 5 feedback --sort + 5 digest --sort
- aliases.test.ts: +9 (11 -> 20) — 5 --sort + 4 --paths-only
- pins.test.ts: +4 (15 -> 19) — --paths-only family alias
- mutes.test.ts: +4 (12 -> 16) — --paths-only family alias
- related.test.ts: +5 (28 -> 33) — --sort
- Total: +32 new tests across 6 files

## Verify-gate
- `pnpm --filter @clawmind/cli test`: first run hit the known status --check-after timer-race flake on 1 test (`status --watch --json --check-after`; same flake every prior tick). Retry was clean 530/530.
- `pnpm --filter @clawmind/cli typecheck`: clean.
- Telemetry OpenTelemetry 1.x/2.x peer mismatch + rag/hybrid alpha-blend drift: pre-existing, unchanged, both queued for a future tick.

## Roadmap update
Queued list refilled — most of the old queue was redundant after this sweep. New items focus on:
1. Bringing existing pre-family --sort implementations (stats, tags) in line with the new contract.
2. Completing --sort on search + stale (mirror related --sort byte-for-byte).
3. Investigating the status `--check-after` timer-race flake (now happens often enough to deserve a real fix).
4. Three small pin/contract gaps that emerged from this tick.

## Notes for next tick
The --sort family is now complete enough that the next tick could profitably tackle the stats/tags retro-fit to the new contract OR pick up the search --sort gap. Both are well-scoped, small, and high-value.
