# Tick session log — 2026-06-22 13:41 PDT

Five features shipped directly on `main`. Quality gate (`@clawmind/cli`):
564/564 vitest pass (up from 530); typecheck clean. First test run hit
the known status `--check-after` timer-race flake (1 test); retry was
clean 564/564 — same flake every prior tick, unchanged.

## Commits (in order)

1. `5e57ff3` feat(search): --sort <score|path|namespace> orders survivors of -t/--threshold
2. `a8fd3d9` feat(stale): --sort <age|path|size> orders survivors of -q / --since / --days
3. `990689f` feat(digest): show --paths-only flat-emit of new/removed paths from filtered history rows
4. `19da6aa` feat(stats): --sort name family-alias + secondary-by-original-index tie sort
5. `c3f8c56` test(feedback): pin prune --above --below --apply 3-flag byte-layout under --json

Push: `ce05108..c3f8c56 main -> main`. All commits authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`.

## Test count deltas

- search.test.ts: 37 -> 46 (+9; --sort family completion)
- stale.test.ts: 23 -> 31 (+8; --sort family completion)
- feedback-digest.test.ts: 101 -> 112 (+11; 8 digest --paths-only + 3 feedback prune byte-pin)
- stats.test.ts: 52 -> 57 (+5; --sort name alias + 2-test determinism property)
- Total: 530 -> 564 (+34)

## Theme

Two cross-command family contracts completed:

**--sort family** is now COMPLETE across all 9 list-style commands:
feedback list, digest list, aliases list, tags list, related, search,
stale, stats, digest show (via --last newest-first). Uniform contract:
applied AFTER narrowing filters, secondary sort by original index for
ties, unknown keys abort with exit 1, default preserves API order.

**--paths-only family** is now COMPLETE across all 9 list-style
commands: search, forget, related, stale, pins, mutes, aliases, tags,
digest show. Uniform contract: pipeline-friendly emit, no ANSI, no
headers, no empty-state hints (so `| xargs` never gets poisoned),
short-circuits before --json when both are set.

Both families were built one command at a time over the prior 8 ticks;
this tick is the closing-the-gap pass that brings the last few
commands into uniform shape AND adds a pure regression-guard
byte-layout pin for the trickiest 3-flag composition that was
previously only count-asserted.

## Quality gate details

- `pnpm --filter @clawmind/cli test` second run: 564/564 pass (first
  run hit the known status `--check-after` timer-race flake on 1
  test — same flake every prior tick, NOT introduced by anything in
  this batch). Retry was clean.
- `pnpm --filter @clawmind/cli typecheck`: clean.
- Pre-existing reds outside cli: telemetry OpenTelemetry 1.x/2.x peer
  mismatch + rag/hybrid alpha-blend drift. Neither introduced by this
  tick; both verified pre-existing in every prior tick.

## Roadmap state after this tick

Marked done (4 of 13 queued):
- feat(stats): --sort name + tie determinism
- feat(search): --sort <score|path|namespace>
- feat(digest): show --paths-only
- feat(stale): --sort <age|path|size>
- feat(feedback): prune --above --below 3-flag byte pin

Refilled with 5 new items surfaced by this tick's patterns:
- feat(digest): show --paths-only --diff (split newSources / removedSources)
- feat(stale): --sort age --reverse (establish reverse-modifier precedent)
- feat(search): --sort path --reverse (same precedent question)
- feat(related): --sort path --reverse + --sort namespace --reverse (for symmetry)
- feat(feedback): prune --json --slim (drop paths[] for cron dashboards)
- feat(stats): --slim --paths --json composition byte-layout pin
