# Tick 2026-06-23 15:09 PDT — 5 features (Cake/cron)

## Shipped (5)

| SHA | Command | Title |
|-----|---------|-------|
| e16f742 | watch  | --only-add/--only-change/--only-unlink event-kind filter triplet on the live NDJSON stream |
| 5373fc4 | tags   | paths <tag> --sort path + --reverse port to the family-wide --sort/--reverse contract |
| fc45981 | digest | show <id> --json --slim emits {count, addedCount, removedCount} 3-integer churn-history shape |
| 08b52b2 | stats  | --tsv --header prepends the schema row for typed-table parsers (column -t / pandas.read_csv) |
| 50cc616 | stale  | --top <n> caps survivors of every narrowing + ordering filter |

## Test gate

- `@clawmind/cli`: **833/833** vitest pass (up from 785; +48 net new)
  - watch.test.ts: +10 tests
  - tags.test.ts: +9 tests
  - feedback-digest.test.ts: +9 tests
  - stats.test.ts: +8 tests
  - stale.test.ts: +10 tests
- Typecheck: cli clean. Build: cli clean.
- `pnpm run ci:verify`: 19 successful, 1 failed
  - The one failure is the same pre-existing `@clawmind/telemetry` OpenTelemetry 1.x/2.x peer mismatch
    that has been queued since tick 1 — unchanged by this batch.

## Theme

Five different family-wide patterns ported to five different commands. Each commit
closes a queued item from the previous tick AND establishes the pattern on one
more command in the cli surface:

- `--only-*` exclusive-emit pattern (digest -> watch)
- `--sort path` family contract (stats etc. -> tags paths)
- `--json --slim` cron-dashboard shape (digest list/run -> digest show)
- `--tsv --header` schema-row contract (stale -> stats)
- `--top` family-wide post-sort cap (stats etc. -> stale)

## Process notes

- Stale .d.ts/.js sidecars cleared at tick start (recurring macOS quirk from prior ticks).
- Two patches mid-tick had escape-doubling issues — `\'` apostrophes in kleur error
  strings got double-escaped to `\\'` by the patch tool, producing TS1005 syntax errors.
  Fixed by re-patching with the single-escape form. No semantic bug, just a gotcha.
- One test-iteration regression mid-tick: the first version of `tags paths --sort` did
  NOT thread the sorted `paths` into the --json / --slim / text branches — they kept
  reading the unsorted `out.paths`. Fixed by re-threading; 3 failing tests caught it
  cleanly.
- Verified the telemetry red is pre-existing (matches every tick since tick 1).

## Queue status

- 4 of last tick's queued items resolved this tick (compact, watch-once-slim, digest-only-*, tags-paths-sort).
- 4 new follow-up items added: digest --sort ts / addedCount / removedCount, forget --top,
  search --tsv, watch --once --only-* audit (closed as not-needed).
- Queue remains well above the >=5 ready-items floor.

## Push verification

```
git fetch -q origin && git log --oneline origin/main | head -7
50cc616 feat(stale): --top <n> caps survivors of every narrowing + ordering filter
08b52b2 feat(stats): --tsv --header prepends the schema row for typed-table parsers
fc45981 feat(digest): show <id> --json --slim emits {count, addedCount, removedCount}
5373fc4 feat(tags): paths <tag> --sort path + --reverse port to the family-wide --sort/--reverse contract
e16f742 feat(watch): --only-add/--only-change/--only-unlink event-kind filter triplet
1804d63 chore(cron): update STATE roadmap + session log for 2026-06-23 11:25 tick
0ee197f feat(stats): --json --slim --paths re-targets to a flat NAMESPACE-NAME pipeline stream
```

All 5 commits landed on `origin/main` cleanly.
