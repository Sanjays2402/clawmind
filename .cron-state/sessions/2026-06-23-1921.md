# 2026-06-23 19:21 PDT — Cake/cron tick

5 features shipped directly on main: TSV ports + family-wide --sort/--top extensions.

## Commits (oldest first, all on main)

1. `18ad396` feat(search): --tsv [+ --header] emits the family-wide tab-separated rank/path/score/namespace stream
2. `0037057` feat(related): --tsv [+ --header] extends the search-tsv 4-col shape with a 5th `hits` column
3. `e8b79e7` feat(feedback): list --tsv [+ --header] emits the family-wide tab-separated path/boost/ups/downs stream
4. `6755e2f` feat(digest): show --sort <ts|addedCount|removedCount> + --reverse port to the family-wide ordering primitive
5. `c87a78b` feat(forget): --top <n> caps the previewed removedPaths array + --paths-only stream (presentation-only, REJECTED with --apply)

Push: `bb313a1..c87a78b main -> main`. All authored as
`Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`.

## Test gate

- `@clawmind/cli` 878/878 pass (up from 833; +45 net new tests across 4 files).
  - search.test.ts +9 (62 -> 71)
  - related.test.ts +8 (47 -> 55)
  - feedback-digest.test.ts +19 (9 for feedback list --tsv + 10 for digest show --sort)
  - forget.test.ts +8 (25 -> 33)
- `@clawmind/cli` typecheck: clean.
- Full `pnpm run ci:verify`: 19/21 tasks pass. Two pre-existing reds unchanged:
  - `@clawmind/telemetry` OpenTelemetry 1.x/2.x peer mismatch (queued since tick 1)
  - `packages/rag/test/hybrid.test.ts` alpha-blend drift (queued since tick 6)

## Theme — TSV family-wide port + two queued sweep closes

Three different family-wide patterns ported to five commands this batch:
1. **--tsv [+ --header]** family-wide tab-separated emit: stats -> search, related, feedback list. The cli now has FIVE commands carrying the same TSV contract (stale, stats, search, related, feedback list) with consistent schema-row --header semantics, byte-for-byte cross-mode precision (.toFixed(3) on scores, .toFixed(2) on boost), and columns that MIRROR the corresponding --json --slim shape so downstream parsers flip framing without schema drift.
2. **--sort + --reverse** family-wide ordering primitive ported to digest show. Three keys (ts/addedCount/removedCount), all desc by default; --reverse flips with the dual sign-flipping multiplier; ties carry secondary index sort. Composition pin: `--sort addedCount --last 1` surfaces the LOUDEST recent ingest event (not the newest) — the operator-friendly cron one-liner.
3. **--top** family-wide post-set cap ported to forget WITH a safety twist: forget --top is REJECTED with --apply because allowing a cap on the destructive path would silently delete the FULL match while only showing the operator the head N. The safety contract is the headline pin of this feature.

## Process notes

- Patch-tool gotcha: first attempt at search --tsv help text crammed two `.option()` calls onto one line because embedded backslashes confused the patch tool's escape handling. Typecheck caught immediately; fixed by re-patching with simpler help (no shell-escape gymnastics). Multi-line .option() chains with embedded `\\` literals remain fragile to the patch tool.
- During the search --tsv patch the entire `--slim` option declaration was accidentally deleted; the failing 9 test cases caught it cleanly within 30 seconds. Restored with a targeted re-insertion. Per-feature test gate before commit prevented this from landing on main.

## Queue refill

7 new follow-up items added (related --tsv --slim composition pin, stats slim --tsv --header zero-row pin, digest show --paths-only --sort, feedback prune --tsv, search --tsv --out file dispatch, forget --top proportional cap, digest show --tsv). Queue stays well above the ≥5 ready-items floor for the next tick.

## Theme connector

This is the seventeenth consecutive ship-from-patterns tick. The cli's pipeline-contract surface is now significantly stronger:
- --tsv contract spans 5 commands uniformly with byte-stable cross-mode precision
- --sort+--reverse contract spans 8 commands uniformly (added digest show)
- --top contract spans 7 commands uniformly (added forget, with safety guard for the destructive path)

Sanjay reviews these later.
