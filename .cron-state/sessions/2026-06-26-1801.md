# Tick 2026-06-26 18:01 PDT - design-language re-theme batch (5 slices)

Cake (cron), 20-min batch loop, FRONTEND override active.

## What shipped (5 full re-themes, all on main, gated green)

Pushed dc29d9c..00913a6:

1. **85debc9** feat(web/settings/invitations): re-theme onto the cm palette
   - Off --bg/--surface/--border/--fg/--muted + amber/emerald/rose status chips.
   - Status chips -> brand feedback inks (pending=cite gold, accepted=--cm-success,
     revoked=--cm-danger, expired=neutral), matching the search-chip precedent 95f55d4.
   - One-time token reveal panel -> cite-gold caution; action banners -> success/danger.

2. **61bd735** feat(web/settings/policies): re-theme onto the cm palette
   - Off --surface/--surface-hover/--warning-bg + green-600/red-600.
   - Accept + Publish became bg-cm-fg/text-cm-bg ink buttons (were plain bordered).
   - "Action required" unmet-policy banner -> cite-gold caution surface; accepted
     chip -> --cm-success; required checkbox accent tint.

3. **6041d3e** feat(web/posture): re-theme the security-posture scorecard onto cm
   - Off --bg/--card/--border + emerald/amber/rose.
   - Two lookup maps (STATUS_INK / STATUS_TINT) drive every pass/warn/fail surface
     (dots, badges, score tone, 3 count cards, configured line) through
     --cm-success / cite gold / --cm-danger so they can't drift.

4. **65f98fb** feat(web/settings/security): re-theme IP allowlist off shadcn onto cm
   - Off bg-background/bg-card/text-muted-foreground/bg-primary/border-input/ring-ring.
   - Enforce toggle "on" -> brand accent; caution hint -> cite gold; errors -> --cm-danger;
     Saved -> --cm-success; Save -> ink button.
   - Wired SettingsCardSkeleton rows=4 for the loading state (completes c0f0ad9 deferral).

5. **00913a6** feat(web/settings/notifications): re-theme off shadcn onto cm
   - Same shadcn palette off; toggle "on" -> accent, off -> bg-cm-subtle.
   - Swapped the bespoke animate-pulse loading list for SettingsCardSkeleton rows=4.

## Gate

- `pnpm --filter @clawmind/web typecheck` -> exit 0
- `pnpm --filter @clawmind/web build` -> exit 0 (all 5 re-themed routes compiled:
  /posture, /settings/security, /settings/notifications, /settings/invitations,
  /settings/policies)
- ci:verify NOT run as the whole-repo gate because packages/telemetry carries a
  known pre-existing red (OpenTelemetry resources v2 migration) tracked in STATE.md;
  the web batch is gated independently per the FRONTEND-override convention.

## Key finding

The drift is REAL, not cosmetic. Grepped the whole repo: --bg / --surface /
--surface-hover / --card / --muted / --fg / --border / --bg-elev / --muted-fg AND
the shadcn bg-card / text-muted-foreground / bg-primary / border-input / ring-ring
family are DEFINED NOWHERE (only --cm-* ships, in packages/ui/src/styles/tokens.css).
So these pages rendered on broken CSS fallbacks: "muted" copy = full-strength navy
ink, every card/list/form panel = no surface fill, status chips = washed-out
Tailwind colors on the warm paper-cream theme. Re-theming is genuine UX repair.

## Remaining work (refilled roadmap)

~28 pages still on the shadcn palette + ~25 on the foreign --var palette (~50 total).
Refilled STATE.md with three coherent re-theme batches (A: access/identity, B:
foreign-var settings, C: compliance/legal) plus the carried chat + global-polish
items. Documented the mechanical foreign-var->cm-* map and the brand-ink rules so
future ticks move fast.
