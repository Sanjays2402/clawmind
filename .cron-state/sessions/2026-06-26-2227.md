# Session 2026-06-26 22:27 PDT - cm-* utility-layer FIX + access/identity re-theme

Cake (cron), 20-min batch. FRONTEND override active. 5 slices, gated once,
pushed to main 6483cc4..ae5b3dd. Working tree clean, local == origin @ ae5b3dd.

## What shipped

The standing theme is the DESIGN-LANGUAGE re-theme (~50 pages off foreign
palettes onto cm-*). Before re-theming the next batch I studied the design
tokens + a shipped reference page and discovered the re-theme work has been
shipping onto utility classes THAT NEVER COMPILED. So slice 1 fixed the root
cause; slices 2-5 re-themed batch A onto the now-working utilities.

1. **920bf7b fix(web/theme): register cm-* palette as Tailwind colors** -
   ROOT-CAUSE fix. globals.css only ever hand-rolled a small base subset of
   cm-* helpers (.bg-cm-paper / .text-cm-muted / ...), no variants. Tailwind v4
   silently drops unknown utilities, so bg-cm-fg + text-cm-bg (the ink buttons,
   16+15 sites), hover:text-cm-fg (108x), divide-cm-border (30x),
   focus:ring-cm-accent (35x), placeholder:text-cm-muted, bg-cm-danger, etc.
   all rendered as no-ops while the build stayed green. Registered the full
   --cm-* palette under @theme as --color-cm-* (pointing at the CSS vars, so
   theme-aware/dark-flip). Verified against compiled CSS: 15 formerly-missing
   utility families incl. all variants now emit real rules; rgba tokens emit
   clean var() with no broken opacity wrapping. Repairs every already-shipped
   re-theme app-wide.
2. **93b25e6 feat(web/settings/members)** - re-theme RBAC admin; ROLE_TONE
   privilege gradient (owner=accent, admin=cite gold, member/viewer=
   neutral/muted); ink buttons; success/danger banners.
3. **4fd8d2b feat(web/settings/sessions)** - current session bg-cm-accent-soft
   wash + accent "This browser" chip; revoke + bulk revoke -> --cm-danger.
4. **2b92191 feat(web/settings/sign-in-log)** - OutcomeBadge through brand inks
   (success=green, failure=cite-gold caution, logout=neutral); failure reason +
   admin-block notice -> cite caution.
5. **ae5b3dd feat(web/settings/sign-in-anomalies)** - StatusBadge Open ->
   cite-gold caution, Acknowledged -> --cm-success.

## Gate

- web typecheck: GREEN. web build: GREEN. ui build: GREEN. api typecheck: GREEN.
- ci:verify: red ONLY on @clawmind/telemetry typecheck (OTel sdk-trace-base
  2.7.1 vs 1.28.0 version-skew). Confirmed IDENTICAL on pre-tick 6483cc4 and
  never touched this tick (my 5 commits touch only apps/web). Pre-existing,
  unrelated, documented across many prior ticks.
- Verified the 5 routes' page chunks built + the formerly-broken utilities
  they use are present in the final CSS.

## Notes for next tick

- cm-* utilities NOW COMPILE incl. all variants - future re-themes can use
  bg-cm-fg/text-cm-bg ink buttons + hover:/focus:/divide- variants freely.
- Batch A is 4/5 done; access-reviews CARRIED (richest feedback surface -
  decision badges + campaign chips + ActionButton tone matrix; mapping noted
  in STATE roadmap). Next up: finish access-reviews + start BATCH B (foreign
  --var settings: encryption/sso/mfa/maintenance/workspace-freeze).
- The hand-rolled .bg-cm-* helper block in globals.css (~lines 297-315) is now
  redundant (superseded by @theme) but left in place; a future cleanup could
  delete it once confirmed nothing depends on the exact specificity.
