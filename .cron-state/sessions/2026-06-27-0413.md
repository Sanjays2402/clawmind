# Session 2026-06-27 04:13 PDT - RE-THEME BATCH C (compliance/legal settings)

5 frontend slices shipped, gated green, pushed to main `18973ea..135bcf0`.

## What shipped
The five compliance/legal settings pages, re-themed off the foreign palettes
onto the cm-* brand. DPA was the lone page in the shadcn language; the other
four were on the --var palette. Same root finding as every prior re-theme tick:
none of --bg/--surface/--surface-2/--card/--border/--muted/--fg/--accent nor the
shadcn bg-card/text-muted-foreground/bg-primary/bg-background/border-border/
bg-muted family are defined anywhere in the app (only --cm-* ships in
tokens.css), so all five rendered on broken fallbacks.

- 06c896e feat(web/settings/legal-hold) - off --var; ACTIVE-hold banner ->
  cite-gold caution (intentional pause, not a hard fail), no-hold ->
  --cm-success; Impose/Update -> bg-cm-fg ink; Release -> neutral cm-border;
  inputs on bg-cm-bg + focus:ring-cm-accent; bare Spinner -> SettingsCardSkeleton.
- 7b9adbb feat(web/settings/sub-processors) - off --var; active chip + Saved ->
  --cm-success 10%% tint, actionError -> cite caution, Retire -> --cm-danger;
  Save/Disclose -> bg-cm-fg ink (were bg-[accent] text-white); shared INPUT_CLS.
- 15c5924 feat(web/settings/ropa) - Article 30 register off --var; legal-basis
  select + every field on INPUT_CLS; active chip + Saved -> --cm-success, error
  -> cite caution, Retire -> --cm-danger; dl keys text-cm-fg over muted values.
- 23df922 feat(web/settings/dpa) - the one shadcn page; status banner is now
  state-driven (accepted+current -> --cm-success surface, stale/none -> cite-gold
  caution); Record acceptance -> bg-cm-fg ink (was bg-primary text-primary-
  foreground); version/ledger dividers -> divide-cm-border; <pre> body + code ->
  bg-cm-subtle; per-row Verify -> --cm-success ok / --cm-danger mismatch.
- 135bcf0 feat(web/settings/erasure-certificates) - off --var; sig-ok pill ->
  --cm-success, mismatch/revoked + row error -> --cm-danger; cm-paper rows.

## Gate (once for the batch)
- web typecheck: GREEN (tsc --noEmit)
- web build: GREEN - all routes compiled, incl. /settings/dpa (3.66 kB),
  /erasure-certificates (2.17 kB), /legal-hold (2.49 kB), /ropa (3.73 kB),
  /sub-processors (3.01 kB).
- @clawmind/ui build: GREEN.
- Batch diff is exactly 5 files, all under apps/web/src/app/settings/, 0 telemetry.
  ci:verify's only red remains the pre-existing, unrelated @clawmind/telemetry OTel
  SDK version-skew (never touched this tick).

## State
- Marked batch C [x] in STATE.md with per-slice SHAs.
- Added TICK LOG 2026-06-27 04:13 PDT.
- Refilled the roadmap: batches D (workspace/account --var: whoami, retention,
  warrant-canary, workspace-deletion, scim), E (access-policy --var:
  session-policy, mfa-policy, offboarding, domains, api-key-bruteforce), F
  (API-key shadcn: api-key-policy, api-key-expiry, api-key-inactivity, quota,
  model-allowlist). ~36 settings pages still drift (11 --var, 25 shadcn).

NEXT TICK: BATCH D. After D, the chat per-message history is still the biggest
product gap - pull it in if a re-theme batch doesn't cohere.
