# Session 2026-06-27-1308 PDT - RE-THEME BATCH E: access-policy settings

Cron 20-min batch loop, FRONTEND override active. Shipped 5 re-theme slices moving
the access-policy settings pages off the foreign --var palette (and assorted raw
emerald/amber/red status surfaces) onto the brand cm-* utilities. This batch
essentially EXHAUSTS the --var cluster - only /settings/dsr carries stray --var
tokens after this tick; remaining drift is the ~25-page shadcn palette.

## Shipped (pushed fe51b45..dd20e56 on main)

1. 2620186 feat(web/settings/session-policy): off --surface-1/2/--accent/--text-muted;
   NEW state-driven live banner (any cap active -> --cm-success wash, no cap at all ->
   --cm-cite gold caution so an unbounded session policy reads as the posture gap it
   is); INPUT_CLS hoist on the 3 numeric inputs; ink Save button; bare Spinner ->
   SettingsCardSkeleton rows=4.
2. 7e142a8 feat(web/settings/mfa-policy): off --bg/--muted-fg/--card/--accent + raw
   emerald/amber; enforced banner -> --cm-success wash, off -> --cm-cite caution;
   grace input INPUT_CLS; Enable/Update -> ink, Disable + action error -> --cm-danger;
   Saved -> --cm-success; skeleton.
3. 286290f feat(web/settings/offboarding): off --bg/--border/--bg-elev/--fg-muted;
   section count chip now state-driven (0 orphans -> --cm-success healthy, any ->
   --cm-cite caution); per-orphan Revoke -> legible --cm-danger button; action-error
   danger / revoked-confirmation success banners; brand icon tile; skeleton rows=3.
4. 8eb228c feat(web/settings/domains): off --bg/--border/--card/--surface/--surface-
   hover/--accent + raw amber-600; FIELD_CLS hoist (domain input + role select); ink
   Save; --cm-danger save-error surface; Saved -> --cm-success; brand-accent
   checkboxes; remove-button danger hover; skeleton rows=4.
5. dd20e56 feat(web/settings/api-key-bruteforce): off --bg/--border/--surface/--bg-elev
   + heavy raw amber/emerald light+dark; NEW at-a-glance posture chip (locked now ->
   --cm-danger "Lockout active", else --cm-success "Armed and clear"); eventChipClass
   routes throttle-log chips through state (lock=danger incident, unlock=success
   recovery, fail=neutral); locked row badges + locked summary count -> --cm-danger,
   tracking rows -> --cm-cite; unlock-error/cleared banners danger/success; skeleton.

## Gate (ONCE for the batch)

- web typecheck GREEN (tsc --noEmit clean).
- @clawmind/ui build GREEN.
- web build GREEN - all routes compiled, incl. all 5 batch routes
  (/settings/session-policy, /mfa-policy, /offboarding, /domains, /api-key-bruteforce).
- Batch diff = exactly 5 files, all under apps/web/src/app/settings/, 0 telemetry.
  ci:verify's only red remains the pre-existing, unrelated @clawmind/telemetry OTel
  SDK version-skew - NEVER touched this tick.

## Notes for next tick

- The --var cluster is essentially done. Only /settings/dsr still has stray --var
  (mostly shadcn). 25 shadcn-palette pages remain (bg-card/text-muted-foreground/
  bg-primary/bg-background/border-border/bg-muted).
- BATCH F (API-key policy, shadcn palette: api-key-policy, api-key-expiry,
  api-key-inactivity, quota, model-allowlist) is queued in STATE.md and coheres as
  a group - that's the obvious next 5.
- Gold reference for cm-* patterns: settings/retention (INPUT_CLS, cm surfaces,
  ink/danger buttons, SettingsCardSkeleton) and settings/scim (cite-gold caution
  surface + state-driven status chip).
- The CHAT SURFACE per-message history item remains the single biggest product gap;
  pull it in if a shadcn re-theme batch doesn't cohere.
