# Session 2026-06-27 18:10 PDT - RE-THEME BATCH F: API-key policy settings

Cake (cron), 20-minute autoship loop. FRONTEND override active.

## Shipped 5 slices (one commit each), pushed be647cb..f69ddb1

BATCH F - the five API-key-policy + usage settings pages, all on the foreign
shadcn palette (bg-background / bg-card / text-muted-foreground / bg-primary
text-primary-foreground / border-border / destructive / focus:ring-ring). None
of those tokens are defined anywhere in the app, so all five rendered on broken
fallbacks; several also carried hand-rolled raw red/amber status surfaces. Each
slice got a genuine state-driven UX upgrade on top of the mechanical shadcn->cm map.

- 28486c6 feat(web/settings/api-key-policy): off shadcn; NEW enforcement banner -
  activeCaps() counts how many of the 6 caps (maxTtl, requireExpiry, maxActiveKeys,
  maxScopes, no-wildcard, forcedRotation) constrain new keys; any active -> success
  wash, all-zero -> cite-gold "Unrestricted" caution so the posture gap reads.
  INPUT_CLS hoist; ink Save; Clear-all-caps -> --cm-danger; Saved -> --cm-success
  surface; brand-accent checkboxes; SettingsCardSkeleton replaces the bare Spinner.
- 64106b0 feat(web/settings/api-key-expiry): off shadcn + raw red/amber; urgencyClass
  routes the per-key countdown through brand inks (<=1d -> --cm-danger, <=7d ->
  --cm-cite, else muted); "expiring soon" stat card becomes a state-driven cite
  surface; warnDays=0 hint flips to cite. INPUT_CLS; ink Save; Saved -> success; skeleton.
- 3f95aad feat(web/settings/api-key-inactivity): off shadcn + raw red/amber; the three
  count cards state-driven (Approaching -> cite, Past-threshold -> danger); destructive
  Sweep button + post-sweep result through --cm-danger / --cm-success; at-risk status
  pills (expired -> danger, warn -> cite); idleDays=0 hint -> cite. INPUT_CLS; ink Save.
- 9448a49 feat(web/settings/quota): off shadcn; NEW three-state usage-health banner +
  colored progress meter - quotaHealth() derives over(>=100%, 429ing) -> --cm-danger,
  near(>=80%) -> --cm-cite, healthy -> --cm-success from this month vs the effective
  ceiling; unlimited skips it. INPUT_CLS; ink Save; field errors + Saved -> brand inks; skeleton.
- f69ddb1 feat(web/settings/model-allowlist): off shadcn + raw amber/red; active mode
  button -> bg-cm-accent-soft wash + accent border; empty-allow-list warning off raw
  amber -> --cm-cite caution; per-model Remove -> --cm-danger hover wash; ink Add;
  action errors -> --cm-danger, Saved -> --cm-success. INPUT_CLS; skeleton.

## Gate (ONCE for the batch)

- web typecheck: GREEN (tsc --noEmit, 0 errors)
- @clawmind/ui build: GREEN
- web build: GREEN - all routes compiled, incl. the 5 BATCH F routes
  (/settings/api-key-policy 2.95kB, /api-key-expiry 2.5kB, /api-key-inactivity
  3.23kB, /quota 2.92kB, /model-allowlist 2.79kB)
- Batch diff: exactly 5 files, all under apps/web/src/app/settings/, 0 telemetry.
  ci:verify's only red remains the pre-existing, unrelated @clawmind/telemetry
  OTel SDK version-skew - never touched this tick.

## Next tick

BATCH F was the LAST of the queued A-F re-theme batches. Refilled STATE.md with
BATCH G (allowlist/network: webhook-allowlist, webhook-events-allowlist,
workspace-ip-allowlist, workspace-origin-allowlist, sign-in-geofence) + BATCH H
(access/trust: role-elevation, recovery-contacts, vendor-access, share-policy,
trust) + a top-level-pages cluster (webhooks, usage, stats, shares, stale). The
CHAT SURFACE per-message history item is still the biggest remaining product gap.
