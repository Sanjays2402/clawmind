# Session 2026-06-27 22:59 PDT - RE-THEME BATCH G (allowlist/network settings)

Cron 20-min batch tick. Lock held (TICK_PROCEED). Worked directly on `main`.

## Shipped: 5 frontend slices (1 commit each), gated ONCE, pushed 883521e..5e39cbf

The five allowlist/network settings pages still on the foreign shadcn palette
(bg-background / bg-card / text-muted-foreground / bg-primary text-primary-foreground /
border / border-input / ring-ring / destructive / accent-foreground) plus hand-rolled
raw emerald/amber/red status surfaces. Confirmed REAL not cosmetic (same finding as
every prior re-theme tick): those shadcn tokens are defined NOWHERE in the app - only
--cm-* ships in tokens.css + the @theme color bridge in globals.css - so all five
rendered on broken fallbacks (no surface fill, no brand CTA fill, no theme-aware focus
ring, no brand accent on toggles/checkboxes).

Each slice got a genuine state-driven UX upgrade on top of the mechanical shadcn->cm map:

1. **b5ff671 webhook-allowlist** - NEW 3-state enforcement-posture banner:
   success wash when on + covering N hosts, cite-gold gap when on but empty (blocks
   every delivery), muted when off. INPUT_CLS hoist; --cm-accent toggle; Remove ->
   --cm-danger hover wash; ink Save; SettingsCardSkeleton rows=4.

2. **c943561 webhook-events-allowlist** - NEW data-exposure posture banner counting
   how many approved subjects carry user content (ask.completed/audit.event) -> cite
   gold when sensitive subjects reachable / --cm-success when metadata-only. Per-event
   rows state-driven: approved -> bg-cm-accent-soft wash + Approved accent chip;
   sensitive subjects -> gold Sensitive chip. Checkboxes -> accent-cm-accent; skeleton.

3. **c3734b2 workspace-ip-allowlist** - NEW lockdown posture banner (locked->success /
   enforced-but-no-range -> cite "would reject every request" gap / open->muted). Active
   CIDR rows on cm-paper, empty draft rows dim to cm-subtle. Break-glass self-lockout
   confirm moved off raw amber onto the cite-gold caution surface. INPUT_CLS; ink Save.

4. **c63ae1a workspace-origin-allowlist** - NEW CORS posture banner with ADDITIVE
   semantics: enforced+origins->success, enforced+empty->cite baseline-only
   (deliberately NOT flagged as a hard gap like IP, since it falls back to the vendor
   baseline), off->muted. Active-row wash; ink Save; Remove -> --cm-danger.

5. **5e39cbf sign-in-geofence** - country chips state-driven by mode: allow ->
   --cm-success permit set, block -> --cm-danger deny set (helper copy switches too).
   Live server-probe decision chip off raw emerald/destructive onto success/danger
   pill surfaces; mode radio cards selected -> accent; self-lockout 422 confirm ->
   --cm-danger; INPUT_CLS; ink Save.

## Gate (ONCE for the batch)
- web typecheck GREEN (tsc --noEmit clean)
- @clawmind/ui build GREEN
- web build GREEN - all routes compiled, incl. the 5 (/settings/webhook-allowlist,
  /webhook-events-allowlist, /workspace-ip-allowlist, /workspace-origin-allowlist,
  /sign-in-geofence)
- Batch diff is exactly 5 files, all under apps/web/src/app/settings/. 0 telemetry
  touched (ci:verify's only red remains the pre-existing, unrelated
  @clawmind/telemetry OTel SDK version-skew).

## Next tick
BATCH G done. BATCH H (access/trust: role-elevation, recovery-contacts, vendor-access,
share-policy, trust) is queued and is the next coherent cluster - pull it. After H,
the TOP-LEVEL PAGES cluster (webhooks/usage/stats/shares/stale) still drifts on shadcn,
then the CHAT SURFACE per-message history item remains the biggest standing product gap.
