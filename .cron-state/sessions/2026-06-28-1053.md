# Session 2026-06-28 10:53 PDT - TOP-LEVEL PAGES batch I (5 slices)

Frontend override active. First batch of the post-/settings cluster: top-level
pages outside /settings.

## What shipped (pushed 081f543..3c5c4c4 on main)

1. **068d473 feat(web/webhooks)** - re-theme off dead `bg-cm-panel` (5 surfaces)
   onto `bg-cm-paper`; raw emerald/amber/rose -> brand inks (active dot + delivery
   OK -> success, failing count + delete hover + delivery error -> danger, rotate
   dialog + rotating badge -> cite gold). NEW state-driven delivery-health banner
   beside the "Your webhooks" header: any failureCount>0 -> danger "N failing"
   (outranks all), else any active -> success "N live", else all paused -> muted.

2. **4ba00c6 feat(web/shares)** - re-theme off dead `bg-cm-bg-soft` onto
   `bg-cm-paper`; raw red Expired badge (red-200/50/700 + dark:) + Revoke button
   -> `--cm-danger` 10% tint. NEW expiry-posture chip set replacing the bare
   share/view count: open-ended (live, no expiry -> cite gold, most exposed) /
   time-boxed (will expire -> success) / expired (dead, safe to revoke -> muted).

3. **36cbfb5 feat(web/stale)** - data-viz upgrade (page was already cm-themed):
   threshold-RELATIVE severity (>=4x -> severe danger / >=2x -> moderate cite /
   else mild accent), per-row drift bars scaled to the oldest in the set,
   severity-inked clock + age, "N severely drifted" danger summary chip.

4. **98fc045 feat(web/tags)** - NEW proportional usage bars (width = count/max)
   + frequency-desc ranking; flat count-chip grid -> scannable frequency map.

5. **3c5c4c4 feat(web/related)** - NEW similarity-strength bars in citation gold
   (width = score/top-score) so embedding closeness reads as a rankable shape.

## Key decision: audited the queue before trusting it

The queued plan was webhooks/usage/stats/shares/stale. Grep-audited each against
the compiled palette FIRST. Found usage + stats are ALREADY fully cm-* (0 drift;
usage's lone text-white is on bg-cm-accent = house CTA pattern, 32 uses app-wide,
NOT drift). Re-theming them = padding. So: shipped the 2 genuinely-drifted pages
(webhooks, shares), upgraded stale with data-viz, and pivoted the 2 freed slots
to genuine data-viz on tags + related. 5 real slices, 0 filler.

## Confirmed-real drift (verified, not guessed)

`bg-cm-panel` + `bg-cm-bg-soft` are DEAD tokens - defined nowhere (only --cm-*
ships in tokens.css + the --color-cm-* @theme bridge). Both pages rendered with
no surface fill on broken fallback. Same finding-class as every prior re-theme tick.

## Gate (once, for the batch)

- web typecheck GREEN (`tsc --noEmit`)
- web build GREEN - all 5 routes compiled (/webhooks 4.29kB, /shares, /stale,
  /tags, /related)
- @clawmind/ui build GREEN
- Batch diff = exactly 5 files, all under apps/web/src/app/, 0 backend/packages,
  0 telemetry. ci:verify's only red remains the pre-existing unrelated
  @clawmind/telemetry OTel SDK version-skew (never touched this tick).

## Next tick

Top-level pages are now audited + effectively drift-free across the app. The
**CHAT SURFACE per-message conversation history** (ChatShell loses prior Q/A on a
new question) is now the clear single biggest standing product gap - PULL IT NEXT.
After that, the data-viz vein proven this tick (proportional bars scaled to a set
max, state-driven posture chips) is a strong evergreen for the remaining
list/dashboard pages - see the refilled DATA-VIZ VEIN queue in STATE.md
(sources feedback boosts, history per-day model-mix, pins recency).
