# Session 2026-06-28 14:37 PDT - CHAT MULTI-TURN + data-viz vein (5 slices)

Lock acquired 14:37:27 PDT. FRONTEND override active. Worked directly on main.

## What shipped (pushed 34e3293..72c9109)

Pulled the long-flagged centerpiece - CHAT SURFACE per-message conversation
history - as the anchor, plus 3 data-viz-vein upgrades and 1 chat companion.

1. **006d868 feat(web/chat): per-message conversation history within a thread**
   The core gap (verified by reading ChatShell.tsx): the chat held ONE
   answer/sources/question and submit() wiped all of it on every new question,
   so a follow-up erased the prior Q/A and the question lived only in the
   composer (gone on submit). Replaced with a `turns: Turn[]` model
   {id, question, answer, sources, error, done}. submit() prepends a turn and
   streams into it; whole thread stays on screen + scrollable. Top-anchored
   composer -> newest renders directly beneath it (where the old single answer
   sat), scroll DOWN into history (deliberate divergence from the queue's
   "newest at bottom", which assumed a bottom composer). Per-turn serif question
   header (now persistent), answer, Copy/Share, error/retry. Single sticky rail
   follows the ACTIVE turn; [ ] cite cycle + j/k rail scope to it. New thread reset.

2. **9884478 feat(web/sources): diverging boost signal bar**
   Raw "boost +0.40" text -> diverging bar around a centre line (success right /
   danger left, scaled to strongest |boost| in view), numeric value in tabular-nums.

3. **ea6c876 feat(web/history): per-day model-mix strip**
   Bare day count -> segmented model-mix strip (one band/model, width=share,
   widest first, ordered brand-ink palette). groupByDay already supplies items.

4. **e74ccca feat(web/pins): per-row recency lane**
   pinnedAt text -> left vertical recency rail (freshest fills solid accent,
   oldest a 12%-floored sliver) + "newest" badge.

5. **72c9109 feat(web/chat): thread outline navigator**
   Companion the multi-turn stack creates the need for: floating bottom-left
   "Thread N/M" pill -> index of questions, jump+scroll+activate an exchange,
   active marked, per-row source count. Hidden for <2 exchanges. New component
   ThreadOutline.tsx + stable cm-turn-<id> anchors with scroll-margin.

## Gate (ONCE for the batch)
- web typecheck: GREEN
- web build: GREEN - all 102 routes compiled, incl. /chat (10.8kB, up with
  multi-turn+outline), /sources, /history, /pins.
- @clawmind/ui build: GREEN (echo-ok no-op).
- Batch diff: 4 page/component files under apps/web/src + 1 new component. 0
  backend/packages, 0 telemetry.
- Lone build warning (history line 624 sources->useMemo) is PRE-EXISTING in
  HistoryRow (identical at line 552 pre-tick; only shifted by my ModelMixBar
  insertion above it). NOT introduced this tick.

## Notes for next tick
- The #1 standing product gap (chat multi-turn) is now CLOSED.
- Remaining chat-surface item: composer drag-and-drop pin (needs a minimal API
  touch to thread the pinned path into the ask request).
- Data-viz vein refilled (saved delta bar, usage ask/search split, collections
  size map, dashboard audit). Welcome 3-card tour DROPPED from queue (already a
  complete real flow, not a stub - would be padding).
