# Contributing

ClawMind is a personal project but pull requests are welcome.

- Use `pnpm changeset` if you change package interfaces.
- Run `pnpm test` and `pnpm typecheck` before opening a PR.
- Keep commits scoped to a single change.
- Match the existing code style. Prettier and ESLint will yell at you otherwise.
