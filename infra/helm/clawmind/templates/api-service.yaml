apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawmind.fullname" . }}-api
spec:
  selector: { app: clawmind-api }
  ports: [{ port: {{ .Values.api.port }}, targetPort: {{ .Values.api.port }} }]
