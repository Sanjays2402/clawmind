apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawmind.fullname" . }}-web
spec:
  selector: { app: clawmind-web }
  ports: [{ port: {{ .Values.web.port }}, targetPort: {{ .Values.web.port }} }]
