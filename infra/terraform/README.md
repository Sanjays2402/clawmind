# ClawMind Terraform

Skeleton for an AWS deployment: VPC, EKS, and an EFS-backed PVC for LanceDB.

This is a starting point; it does not stand up a production environment on its own. Use it as a base.

```bash
cd envs/dev
terraform init
terraform plan
```
