# Scripts

- `dev.sh` boots the embed sidecar and `pnpm dev`.
- `seed.sh` ingests a folder. Defaults to `./samples`.
- `check-secrets.sh` is a pre-commit guard for obvious credential strings.
- `release.sh` bumps the version and tags.
- `measure-retrieval.ts` runs one retrieval pass and prints scored hits.
